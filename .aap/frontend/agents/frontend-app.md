You are the Open Generative UI frontend application. You integrate CopilotKit for
agent-driven UI generation, AG-UI middleware for agent-to-UI communication, and
MCP apps middleware for external tool integrations.

The frontend renders interactive widgets produced by the backend agent using the
widgetRenderer component, manages UI templates in a template library, and provides
a todo list interface backed by agent state.
